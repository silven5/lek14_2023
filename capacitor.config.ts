import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.lek14_2023',
  appName: 'lek14_2023',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
